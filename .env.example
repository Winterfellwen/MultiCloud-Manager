# Required: Generate a random 64-char hex string
# Linux/Mac: openssl rand -hex 32
# PowerShell: [System.Convert]::ToHexString([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32)).ToLower()
JWT_SECRET=

# Required: Choose a strong password for the admin account
ADMIN_PASSWORD=

# Optional: Change these defaults for production
REDIS_PASSWORD=changeme
POSTGRES_PASSWORD=multicloud_dev
ENCRYPTION_KEY=
