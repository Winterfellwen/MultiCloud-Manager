<view class="resource-card">
  <view class="card-header">
    <text class="resource-name">{{resourceName}}</text>
    <view class="platform-tag platform-{{platform}}">{{platform}}</view>
  </view>
  <view class="card-body">
    <view class="info-row">
      <text class="info-label">{{lang.resource_card_status}}</text>
      <text class="info-value status-{{status}}">{{status}}</text>
    </view>
    <view class="info-row">
      <text class="info-label">{{lang.resource_card_region}}</text>
      <text class="info-value">{{region}}</text>
    </view>
    <view class="info-row">
      <text class="info-label">{{lang.resource_card_spec}}</text>
      <text class="info-value">{{spec}}</text>
    </view>
  </view>
</view>
